#!/usr/bin/env sh

PATH=$PATH:./node_modules/.bin

# Build JS and CSS with source maps
babel src --source-maps --out-dir app/js
node-sass --include-path node_modules --source-map app/css src/scss --output app/css

# Start app
electron app
